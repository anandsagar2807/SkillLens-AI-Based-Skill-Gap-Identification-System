[phases.setup]
nixPkgs = ['python311', 'gcc', 'glibcLocales']

[phases.install]
cmds = ['pip install --no-cache-dir --upgrade pip', 'pip install --no-cache-dir -r requirements.txt']

[phases.build]
cmds = ['python setup.py']

[start]
cmd = 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'
